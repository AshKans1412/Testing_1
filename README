Github Branch for Docker Assignment

> Run the Bash file to create a python environment inside docker
> After the environment is created run the python file with input & output csv file name
> The created data is stored in "BUNKER" section of your docker volume

--

